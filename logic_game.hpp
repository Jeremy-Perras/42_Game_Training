#pragma once
namespace ve {
  class Logic_game {
  public:
    Logic_game();
    Logic_game(const Logic_game &src) = delete;
    Logic_game &operator=(const Logic_game &rhs) = delete;
    ~Logic_game();
    // Getter
  private:
  };
}  // namespace ve