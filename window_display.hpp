#pragma once
namespace ve {
  class WindowDisplay {
  public:
    WindowDisplay();
    WindowDisplay(const WindowDisplay &src) = delete;
    WindowDisplay &operator=(const WindowDisplay &rhs) = delete;
    ~WindowDisplay();
    // Getter
  private:
  };
}  // namespace ve